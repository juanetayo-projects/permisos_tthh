# Copia este archivo como .env y complétalo. NUNCA subas el .env al repositorio.
#
# Desde el 2026-08-02 Permisos tiene **proyecto Supabase propio**. Antes vivía
# dentro del de Cambio de Turnos (decisión D2) y compartía con él `auth.users`,
# `areas`, `cargos` y `coordinadores`; el porqué de la separación está en la
# migración 20260728000100_catalogos_propios.sql.
#
# En el cliente solo va la clave anónima; todo lo privilegiado corre en Edge
# Functions con service_role.

VITE_SUPABASE_URL=https://hvbatymkcpsxhuzkagoi.supabase.co
VITE_SUPABASE_ANON_KEY=
